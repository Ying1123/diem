(declare-const x8 Bool)
(set-option :strings-exp true)
(declare-fun s () (Seq Int))
(declare-fun x () Int)
(declare-fun i () Int)
(declare-fun i@ () Int)
(assert (not (or (exists ((u Int)) (not (or (> 0 u) (>= u i) (distinct x (seq.nth s u))) )) 
                 (and (or x8 (not x8)) 
                      (or (distinct i@ (+ i 1)) 
                          (= x (seq.nth s i)) 
                          (forall ((u Int))  (or (> 0 u) 
                                                 (>= u i@) 
                                                 (distinct x (seq.nth s u))) )))
                     )))
(check-sat)
